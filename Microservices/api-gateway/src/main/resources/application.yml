spring:
  application:
    name: api-gateway
#
#
#  cloud:
#    gateway:
#      routes:
#        - id: firstrest-route 
#          uri: http://localhost:8090
#          predicates:
#            - Path=/loan/api/v1.0/**
#
#
server:
  port: 9999
#  

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
 
  instance:
    hostname: localhost